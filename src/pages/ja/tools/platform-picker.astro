---
import Base from '../../../layouts/Base.astro';
import { SITE, GAME } from '../../../consts';

const jsonLd = [
  {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'ホーム', item: `${SITE.url}/ja` },
      { '@type': 'ListItem', position: 2, name: 'ツール', item: `${SITE.url}/ja/tools` },
      { '@type': 'ListItem', position: 3, name: 'プラットフォーム選択ツール', item: new URL('/ja/tools/platform-picker', SITE.url).href },
    ],
  },
  {
    '@context': 'https://schema.org',
    '@type': 'WebApplication',
    name: 'ムーンライト・ピークス プラットフォーム選択ツール',
    applicationCategory: 'GameApplication',
    operatingSystem: 'Web',
    url: new URL('/ja/tools/platform-picker', SITE.url).href,
    description: 'Steam、Steam Deck、Nintendo Switch、Switch 2、Androidのどれを選ぶか計画するためのムーンライト・ピークス プラットフォーム選択ツール。',
  },
];
---
<Base
  title="ムーンライト・ピークス プラットフォーム選択ツール - Steam、Switch 2、Steam Deck、Androidどれがいい?"
  description="Steam、Steam Deck、Nintendo Switch、Switch 2、Androidのうち、自分に最適な発売時プラットフォームを診断できるツールです。"
  path="/ja/tools/platform-picker"
  jsonLd={jsonLd}
  crumbs={[{ label: 'ツール', href: '/ja/tools' }, { label: 'プラットフォーム選択ツール' }]}
  lang="ja"
>
  <article class="page">
    <div class="wrap">
      <p class="eyebrow">ツール / プラットフォーム選択</p>
      <h1>ムーンライト・ピークス <em>プラットフォーム選択ツール</em></h1>
      <p class="intro">
        {GAME.title}をどこでプレイすべきか迷っていますか?重視する項目を選ぶと、確認済みの
        プラットフォームデータ、体験版の提供状況、慎重な事前情報をもとにおすすめの
        プラットフォームを診断します。
      </p>

      <section class="picker-shell" data-platform-picker>
        <div class="picker-panel">
          <h2>あなたの優先事項</h2>
          <label>
            <span>主なプレイ方法</span>
            <select data-field="playstyle">
              <option value="desk">デスクやノートPCで</option>
              <option value="handheld">主に携帯モードで</option>
              <option value="tv">テレビやソファで</option>
              <option value="mobile">スマホやモバイル端末で</option>
            </select>
          </label>
          <fieldset>
            <legend>何を最も重視しますか?</legend>
            <label><input type="checkbox" value="demo" checked /> 体験版に一番手軽にアクセスしたい</label>
            <label><input type="checkbox" value="performance" /> パフォーマンスとパッチ対応を重視する</label>
            <label><input type="checkbox" value="portable" /> 携帯性を重視したい</label>
            <label><input type="checkbox" value="lowest-price" /> できるだけ安く買いたい</label>
            <label><input type="checkbox" value="controller" /> コントローラー・ソファ操作を好む</label>
          </fieldset>
        </div>

        <div class="picker-result" aria-live="polite">
          <span class="result-kicker">おすすめ</span>
          <h2 data-result-title>Steam</h2>
          <p data-result-copy>素早いアップデート、PC/Macでのプレイ、発売前の最も広い体験版アクセスを求めるなら、Steamが最も無難な選択です。</p>
          <div class="score-list" data-score-list></div>
          <a class="btn btn-primary" data-result-link href="/platforms">プラットフォームガイドを開く</a>
        </div>
      </section>

      <h2>このツールが使用するプラットフォーム情報</h2>
      <table class="facts">
        <tr><th>発売日</th><td>{GAME.releaseLabel}</td></tr>
        <tr><th>確認済みプラットフォーム</th><td>{GAME.platforms.join(', ')}</td></tr>
        <tr><th>体験版</th><td>{GAME.demo}</td></tr>
        <tr><th>価格</th><td>{GAME.price};Switch 2版はより高くなる見込みです。</td></tr>
      </table>

      <div class="callout">
        これは計画用のツールであり、パフォーマンスのベンチマークではありません。最終的な
        Steam Deck認証、Androidの動作要件、Switchのパフォーマンスは発売時に改めて確認してください。
      </div>
    </div>
  </article>

  <script>
    const tools = document.querySelector('[data-platform-picker]');
    if (tools) {
      const platforms = [
        {
          id: 'steam',
          title: 'Steam',
          href: '/platforms',
          copy: '素早いアップデート、PC/Macでのプレイ、発売前の最も広い体験版アクセスを求めるなら、Steamが最も無難な選択です。',
          weights: { desk: 5, handheld: 1, tv: 1, mobile: 0, demo: 3, performance: 3, portable: 0, 'lowest-price': 2, controller: 1 },
        },
        {
          id: 'steam-deck',
          title: 'Steam Deck',
          href: '/platforms/steam-deck',
          copy: 'Steamのエコシステムを維持しながら携帯性を求めるなら、Steam Deckが最適です。',
          weights: { desk: 1, handheld: 5, tv: 1, mobile: 0, demo: 2, performance: 2, portable: 4, 'lowest-price': 1, controller: 2 },
        },
        {
          id: 'switch-2',
          title: 'Nintendo Switch 2',
          href: '/platforms/switch-2',
          copy: '最新のNintendoネイティブ版とソファでのプレイを求めるなら、Switch 2がコンソール系のベストな選択です。',
          weights: { desk: 0, handheld: 4, tv: 5, mobile: 0, demo: 2, performance: 3, portable: 4, 'lowest-price': 0, controller: 4 },
        },
        {
          id: 'switch',
          title: 'Nintendo Switch',
          href: '/platforms',
          copy: 'すでに本体を持っていて、PCを使わずに携帯またはテレビでプレイしたいなら、Nintendo Switchが良い選択です。',
          weights: { desk: 0, handheld: 4, tv: 4, mobile: 0, demo: 2, performance: 0, portable: 4, 'lowest-price': 1, controller: 4 },
        },
        {
          id: 'android',
          title: 'Android / Google Play',
          href: '/platforms/android',
          copy: 'モバイル重視のプレイヤー向けの選択肢ですが、端末の動作要件と操作方法は発売時に確認してください。',
          weights: { desk: 0, handheld: 2, tv: 0, mobile: 5, demo: 0, performance: 0, portable: 4, 'lowest-price': 1, controller: 0 },
        },
      ];

      const playstyle = tools.querySelector('[data-field="playstyle"]');
      const checks = [...tools.querySelectorAll('input[type="checkbox"]')];
      const resultTitle = tools.querySelector('[data-result-title]');
      const resultCopy = tools.querySelector('[data-result-copy]');
      const resultLink = tools.querySelector('[data-result-link]');
      const scoreList = tools.querySelector('[data-score-list]');

      const render = () => {
        const selected = [playstyle.value, ...checks.filter((item) => item.checked).map((item) => item.value)];
        const scored = platforms
          .map((platform) => ({
            ...platform,
            score: selected.reduce((sum, key) => sum + (platform.weights[key] || 0), 0),
          }))
          .sort((a, b) => b.score - a.score);
        const best = scored[0];
        resultTitle.textContent = best.title;
        resultCopy.textContent = best.copy;
        resultLink.href = best.href;
        scoreList.innerHTML = scored
          .map((item) => `<div><span>${item.title}</span><strong>${item.score}</strong></div>`)
          .join('');
      };

      playstyle.addEventListener('change', render);
      checks.forEach((item) => item.addEventListener('change', render));
      render();
    }
  </script>

  <style>
    .picker-shell { display: grid; grid-template-columns: minmax(0, 1fr) minmax(280px, 0.9fr); gap: 18px; margin-top: 28px; align-items: stretch; }
    @media (max-width: 820px) { .picker-shell { grid-template-columns: 1fr; } }
    .picker-panel, .picker-result { border: 1px solid var(--line); border-radius: var(--radius); background: var(--surface); padding: 18px; }
    .picker-panel h2, .picker-result h2 { margin-top: 0; }
    .picker-panel label span, .picker-panel legend { display: block; color: var(--mist); font-weight: 600; margin-bottom: 8px; }
    .picker-panel select {
      width: 100%;
      min-height: 44px;
      color: var(--mist);
      background: var(--ink);
      border: 1px solid var(--line);
      border-radius: 10px;
      padding: 0 12px;
      font: inherit;
    }
    .picker-panel fieldset { border: 0; padding: 0; margin: 22px 0 0; display: grid; gap: 10px; }
    .picker-panel fieldset label { display: flex; gap: 10px; align-items: flex-start; color: var(--muted); }
    .picker-panel input { accent-color: var(--candle); margin-top: 5px; }
    .result-kicker { font-family: var(--mono); font-size: 0.7rem; color: var(--candle); text-transform: uppercase; letter-spacing: 0.12em; }
    .score-list { display: grid; gap: 8px; margin: 18px 0; }
    .score-list div { display: flex; justify-content: space-between; gap: 12px; border-bottom: 1px solid var(--line); padding-bottom: 7px; color: var(--muted); }
    .score-list strong { color: var(--candle); font-family: var(--mono); }
  </style>
</Base>
