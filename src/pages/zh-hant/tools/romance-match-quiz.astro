---
import Base from '../../../layouts/Base.astro';
import { SITE, GAME } from '../../../consts';
import { characters } from '../../../data/characters';

const cast = ['saga', 'luna', 'orlock', 'the-warlock', 'death', 'mina']
  .map((id) => characters.find((c) => c.id === id))
  .filter(Boolean);

const results: Record<string, { tagline: string; blurb: string }> = {
  saga: {
    tagline: '你想要一個能量隨月亮變化的物件。',
    blurb: 'Saga是確認的狼人戀愛物件——開發者描述她的能量會隨滿月變化。如果你想要一個每晚都有真實層次感的伴侶,她是最佳選擇。',
  },
  luna: {
    tagline: '你想要一個腳踏實地、擅長種植又帶點魔法的物件。',
    blurb: 'Luna被描述為務農的女巫——可能是你最早接觸咒語和園藝知識的來源。踏實、實用,大概最瞭解你的作物需要什麼。',
  },
  orlock: {
    tagline: '你想要舊世界的魅力和一點權勢。',
    blurb: 'Orlock被描述為吸血鬼家族的家長——舊世界的影響力,以及那種一進房間就能改變氣場的存在感。',
  },
  'the-warlock': {
    tagline: '你想要自信、魅力,以及毫不掩飾的直白。',
    blurb: '在一次實機試玩報道中,他是個自戀又不停調情的術士。他的真名尚未確認——但那股氣場是真實的。',
  },
  death: {
    tagline: '你想要一個讓全鎮都有點害怕的物件。',
    blurb: 'Death是發售前洩露檔案中出現的名字,還有自己的家("死神小屋")。發售前的一些傳聞暗示他可能是戀愛物件——沒有確認,但如果真的成立,你算是提前猜中了。',
  },
  mina: {
    tagline: '你想要一個謎團,而且不介意暫時無法解開它。',
    blurb: 'Mina被開發者點名提及,但目前幾乎沒有其他公開資訊。有時這正是魅力所在:一張會在發售時被你認識的白紙。',
  },
};

const questions = [
  {
    q: '在小鎮的第一晚,什麼最吸引你?',
    options: [
      { t: '古老的吸血鬼城堡和它的秘密', s: 'orlock' },
      { t: '已經準備好播種的菜地', s: 'luna' },
      { t: '遠處傳來的一聲嚎叫', s: 'saga' },
      { t: '一個別人還沒發現的安靜角落', s: 'death' },
    ],
  },
  {
    q: '抵達五分鐘內就有人對你調情。你會:',
    options: [
      { t: '還是忍不住被迷住了', s: 'the-warlock' },
      { t: '禮貌地推開——你是來工作的', s: 'luna' },
      { t: '用同樣的氣場懟回去', s: 'saga' },
      { t: '想知道他們真正想要什麼', s: 'orlock' },
    ],
  },
  {
    q: '選一棟你想去參觀的房子:',
    options: [
      { t: '一座傳承多代的大家族宅邸', s: 'orlock' },
      { t: '一處安靜整潔、帶點個人風格的家', s: 'orlock' },
      { t: '不管在哪,只要作物長得最好', s: 'luna' },
      { t: '一個全鎮一半人都躲著的地方', s: 'death' },
    ],
  },
  {
    q: '你怎麼看待小鎮七大家族的恩怨?',
    options: [
      { t: '舊世界的政治遊戲——我想站在贏的一邊', s: 'orlock' },
      { t: '不關我的事,我還有農場要打理', s: 'luna' },
      { t: '老實說,還挺刺激的', s: 'the-warlock' },
      { t: '我寧願完全置身事外', s: 'mina' },
    ],
  },
  {
    q: '一輪滿月升起在小鎮上空。你在做什麼?',
    options: [
      { t: '從窗邊望出去,感覺有什麼在變化', s: 'saga' },
      { t: '照料夜間才開的花', s: 'luna' },
      { t: '待在沒人能看見我的地方', s: 'death' },
      { t: '確認月光下我的房子看起來還不錯', s: 'orlock' },
    ],
  },
  {
    q: '鎮上有個人對所有人來說都是個徹底的謎。你的反應:',
    options: [
      { t: '很好奇——我想知道更多', s: 'mina' },
      { t: '很有魅力,但我保持警惕', s: 'the-warlock' },
      { t: '正是我的菜', s: 'death' },
      { t: '我更信任自信和地位', s: 'orlock' },
    ],
  },
];

const faq = [
  { q: '這個測驗是官方的嗎,還是基於真實遊戲資料?', a: '只是娛樂用途。結果選項基於每位角色真實的、有出處的性格線索——開發者公開資訊、實機試玩媒體報道、發售前洩露檔案——但配對本身是我們自己輕鬆的解讀,不是官方的匹配系統。' },
  { q: '這能告訴我誰的禮物或戀愛路線最好嗎?', a: '不能——禮物偏好、戀愛事件和關係機制目前都還沒有任何確認。請檢視我們的戀愛禮物查詢器,瞭解哪些已經驗證、哪些還在等待確認。' },
];

const jsonLd = [
  { '@context': 'https://schema.org', '@type': 'FAQPage', mainEntity: faq.map((f) => ({ '@type': 'Question', name: f.q, acceptedAnswer: { '@type': 'Answer', text: f.a } })) },
  { '@context': 'https://schema.org', '@type': 'BreadcrumbList', itemListElement: [
    { '@type': 'ListItem', position: 1, name: '首頁', item: `${SITE.url}/zh` },
    { '@type': 'ListItem', position: 2, name: '工具', item: `${SITE.url}/zh-hant/tools` },
    { '@type': 'ListItem', position: 3, name: '戀愛契合度測驗', item: new URL('/zh-hant/tools/romance-match-quiz', SITE.url).href },
  ] },
  { '@context': 'https://schema.org', '@type': 'WebApplication', name: 'Moonlight Peaks 戀愛契合度測驗',
    applicationCategory: 'GameApplication', operatingSystem: 'Web',
    url: new URL('/zh-hant/tools/romance-match-quiz', SITE.url).href,
    description: '一個用真實、有出處的性格線索,把你與Moonlight Peaks戀愛物件配對的趣味測驗——不使用編造的契合度資料。' },
];
---
<Base
  title="Moonlight Peaks 戀愛契合度測驗 — 找到你的另一半"
  description="一個有趣又誠實的測驗,用真實、有出處的角色線索,把你與一位Moonlight Peaks戀愛物件配對——不編造禮物或契合度資料。"
  path="/zh-hant/tools/romance-match-quiz"
  jsonLd={jsonLd}
  crumbs={[{ label: '工具', href: '/zh-hant/tools' }, { label: '戀愛契合度測驗' }]}
  lang="zh-hant"
>
  <article class="page">
    <div class="wrap">
      <p class="eyebrow">工具 · 戀愛</p>
      <h1>戀愛<em>契合度</em>測驗</h1>
      <p class="intro">
        六個簡短的問題,對照來自開發者爆料、實機試玩媒體報道和發售前洩露檔案的真實性格線索。
        只是娛樂用途——下方FAQ會解釋這是什麼(以及不是什麼)。
      </p>

      <div id="quiz-root" class="quiz-root">
        <div id="quiz-questions"></div>
        <div id="quiz-result" class="quiz-result" hidden></div>
        <div class="quiz-controls">
          <button type="button" id="quiz-restart" class="rq-btn" hidden>再試一次</button>
        </div>
      </div>

      <h2>FAQ</h2>
      <div class="faq">
        {faq.map((f) => (
          <details>
            <summary>{f.q}</summary>
            <p>{f.a}</p>
          </details>
        ))}
      </div>

      <div class="callout wip">
        相關:<a href="/tools/romance-gift-finder">戀愛禮物查詢器</a>、
        <a href="/romance">戀愛與婚姻</a>、<a href="/characters">全部角色</a>。
      </div>
    </div>
  </article>

  <style>
    .quiz-root { margin: 18px 0 28px; }
    .rq-question { border: 1px solid var(--line); border-radius: 14px; background: var(--surface); padding: 16px; margin-bottom: 14px; }
    .rq-question h3 { font-family: var(--display); font-weight: 600; font-size: 1rem; margin: 0 0 12px; color: var(--mist); }
    .rq-options { display: grid; grid-template-columns: 1fr; gap: 8px; }
    @media (min-width: 640px) { .rq-options { grid-template-columns: 1fr 1fr; } }
    .rq-option {
      text-align: left; font-size: 0.86rem; color: var(--muted); border: 1px solid var(--line);
      border-radius: 10px; background: var(--ink-2); padding: 10px 12px; cursor: pointer;
      transition: border-color .15s ease, color .15s ease, background .15s ease;
    }
    .rq-option:hover { border-color: color-mix(in srgb, var(--candle) 45%, var(--line)); color: var(--mist); }
    .rq-option.is-picked { border-color: var(--candle); color: var(--candle); background: color-mix(in srgb, var(--candle) 10%, var(--ink-2)); }
    .quiz-result { border: 1px solid color-mix(in srgb, var(--candle) 40%, var(--line)); border-radius: 16px; background: var(--surface); padding: 20px; text-align: center; }
    .quiz-result h2 { font-family: var(--display); font-size: 1.3rem; margin: 4px 0 6px; }
    .quiz-result .qr-tagline { color: var(--candle); font-size: 0.94rem; margin: 0 0 12px; }
    .quiz-result .qr-blurb { color: var(--muted); font-size: 0.88rem; line-height: 1.6; max-width: 480px; margin: 0 auto 16px; }
    .quiz-result a.qr-profile { display: inline-block; font-family: var(--mono); font-size: 0.8rem; color: var(--candle); border: 1px solid color-mix(in srgb, var(--candle) 45%, var(--line)); border-radius: 999px; padding: 8px 18px; }
    .quiz-controls { text-align: center; margin-top: 14px; }
    .rq-btn { font-family: var(--mono); font-size: 0.78rem; color: var(--muted); border: 1px solid var(--line); border-radius: 999px; background: var(--surface); padding: 8px 18px; cursor: pointer; }
    .rq-btn:hover { color: var(--candle); border-color: color-mix(in srgb, var(--candle) 45%, var(--line)); }
    .rq-progress { font-family: var(--mono); font-size: 0.68rem; color: var(--faint); margin-bottom: 6px; }
  </style>

  <script define:vars={{ questions, results, cast: cast.map((c) => ({ id: c!.id, name: c!.name })) }}>
    (function () {
      var qWrap = document.getElementById('quiz-questions');
      var resultBox = document.getElementById('quiz-result');
      var restartBtn = document.getElementById('quiz-restart');
      var current = 0;
      var scores = {};

      var profileHref = {};
      cast.forEach(function (c) { profileHref[c.id] = '/characters/' + c.id; });

      function renderQuestion(i) {
        qWrap.innerHTML = '';
        var q = questions[i];
        var box = document.createElement('div');
        box.className = 'rq-question';
        var progress = document.createElement('div');
        progress.className = 'rq-progress';
        progress.textContent = '第 ' + (i + 1) + ' / ' + questions.length + ' 題';
        var h3 = document.createElement('h3');
        h3.textContent = q.q;
        var opts = document.createElement('div');
        opts.className = 'rq-options';
        q.options.forEach(function (opt) {
          var btn = document.createElement('button');
          btn.type = 'button';
          btn.className = 'rq-option';
          btn.textContent = opt.t;
          btn.addEventListener('click', function () {
            scores[opt.s] = (scores[opt.s] || 0) + 1;
            current++;
            if (current < questions.length) { renderQuestion(current); }
            else { showResult(); }
          });
          opts.appendChild(btn);
        });
        box.appendChild(progress);
        box.appendChild(h3);
        box.appendChild(opts);
        qWrap.appendChild(box);
      }

      function showResult() {
        var best = null, bestScore = -1;
        Object.keys(scores).forEach(function (k) {
          if (scores[k] > bestScore) { bestScore = scores[k]; best = k; }
        });
        if (!best) best = 'mina';
        var info = results[best];
        var name = (cast.filter(function (c) { return c.id === best; })[0] || {}).name || best;

        qWrap.style.display = 'none';
        resultBox.hidden = false;
        resultBox.innerHTML =
          '<p class="rq-progress">你的配對</p>' +
          '<h2>' + name + '</h2>' +
          '<p class="qr-tagline">' + info.tagline + '</p>' +
          '<p class="qr-blurb">' + info.blurb + '</p>' +
          '<a class="qr-profile" href="' + profileHref[best] + '">檢視' + name + '的檔案</a>';
        restartBtn.hidden = false;
      }

      restartBtn.addEventListener('click', function () {
        current = 0; scores = {};
        qWrap.style.display = '';
        resultBox.hidden = true;
        restartBtn.hidden = true;
        renderQuestion(0);
      });

      renderQuestion(0);
    })();
  </script>
</Base>
