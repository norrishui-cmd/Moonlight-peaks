---
import Base from '../../../layouts/Base.astro';
import { SITE, GAME } from '../../../consts';
import { characters } from '../../../data/characters';

const cast = ['saga', 'luna', 'orlock', 'the-warlock', 'death', 'mina']
  .map((id) => characters.find((c) => c.id === id))
  .filter(Boolean);

const results: Record<string, { tagline: string; blurb: string }> = {
  saga: {
    tagline: '你想要一个能量随月亮变化的对象。',
    blurb: 'Saga是确认的狼人恋爱对象——开发者描述她的能量会随满月变化。如果你想要一个每晚都有真实层次感的伴侣,她是最佳选择。',
  },
  luna: {
    tagline: '你想要一个脚踏实地、擅长种植又带点魔法的对象。',
    blurb: 'Luna被描述为务农的女巫——可能是你最早接触咒语和园艺知识的来源。踏实、实用,大概最了解你的作物需要什么。',
  },
  orlock: {
    tagline: '你想要旧世界的魅力和一点权势。',
    blurb: 'Orlock被描述为吸血鬼家族的家长——旧世界的影响力,以及那种一进房间就能改变气场的存在感。',
  },
  'the-warlock': {
    tagline: '你想要自信、魅力,以及毫不掩饰的直白。',
    blurb: '在一次实机试玩报道中,他是个自恋又不停调情的术士。他的真名尚未确认——但那股气场是真实的。',
  },
  death: {
    tagline: '你想要一个让全镇都有点害怕的对象。',
    blurb: 'Death是发售前泄露文件中出现的名字,还有自己的家("死神小屋")。发售前的一些传闻暗示他可能是恋爱对象——没有确认,但如果真的成立,你算是提前猜中了。',
  },
  mina: {
    tagline: '你想要一个谜团,而且不介意暂时无法解开它。',
    blurb: 'Mina被开发者点名提及,但目前几乎没有其他公开信息。有时这正是魅力所在:一张会在发售时被你认识的白纸。',
  },
};

const questions = [
  {
    q: '在小镇的第一晚,什么最吸引你?',
    options: [
      { t: '古老的吸血鬼城堡和它的秘密', s: 'orlock' },
      { t: '已经准备好播种的菜地', s: 'luna' },
      { t: '远处传来的一声嚎叫', s: 'saga' },
      { t: '一个别人还没发现的安静角落', s: 'death' },
    ],
  },
  {
    q: '抵达五分钟内就有人对你调情。你会:',
    options: [
      { t: '还是忍不住被迷住了', s: 'the-warlock' },
      { t: '礼貌地推开——你是来工作的', s: 'luna' },
      { t: '用同样的气场怼回去', s: 'saga' },
      { t: '想知道他们真正想要什么', s: 'orlock' },
    ],
  },
  {
    q: '选一栋你想去参观的房子:',
    options: [
      { t: '一座传承多代的大家族宅邸', s: 'orlock' },
      { t: '一处安静整洁、带点个人风格的家', s: 'orlock' },
      { t: '不管在哪,只要作物长得最好', s: 'luna' },
      { t: '一个全镇一半人都躲着的地方', s: 'death' },
    ],
  },
  {
    q: '你怎么看待小镇七大家族的恩怨?',
    options: [
      { t: '旧世界的政治游戏——我想站在赢的一边', s: 'orlock' },
      { t: '不关我的事,我还有农场要打理', s: 'luna' },
      { t: '老实说,还挺刺激的', s: 'the-warlock' },
      { t: '我宁愿完全置身事外', s: 'mina' },
    ],
  },
  {
    q: '一轮满月升起在小镇上空。你在做什么?',
    options: [
      { t: '从窗边望出去,感觉有什么在变化', s: 'saga' },
      { t: '照料夜间才开的花', s: 'luna' },
      { t: '待在没人能看见我的地方', s: 'death' },
      { t: '确认月光下我的房子看起来还不错', s: 'orlock' },
    ],
  },
  {
    q: '镇上有个人对所有人来说都是个彻底的谜。你的反应:',
    options: [
      { t: '很好奇——我想知道更多', s: 'mina' },
      { t: '很有魅力,但我保持警惕', s: 'the-warlock' },
      { t: '正是我的菜', s: 'death' },
      { t: '我更信任自信和地位', s: 'orlock' },
    ],
  },
];

const faq = [
  { q: '这个测验是官方的吗,还是基于真实游戏数据?', a: '只是娱乐用途。结果选项基于每位角色真实的、有出处的性格线索——开发者公开信息、实机试玩媒体报道、发售前泄露文件——但配对本身是我们自己轻松的解读,不是官方的匹配系统。' },
  { q: '这能告诉我谁的礼物或恋爱路线最好吗?', a: '不能——礼物偏好、恋爱事件和关系机制目前都还没有任何确认。请查看我们的恋爱礼物查询器,了解哪些已经验证、哪些还在等待确认。' },
];

const jsonLd = [
  { '@context': 'https://schema.org', '@type': 'FAQPage', mainEntity: faq.map((f) => ({ '@type': 'Question', name: f.q, acceptedAnswer: { '@type': 'Answer', text: f.a } })) },
  { '@context': 'https://schema.org', '@type': 'BreadcrumbList', itemListElement: [
    { '@type': 'ListItem', position: 1, name: '首页', item: `${SITE.url}/zh` },
    { '@type': 'ListItem', position: 2, name: '工具', item: `${SITE.url}/zh/tools` },
    { '@type': 'ListItem', position: 3, name: '恋爱契合度测验', item: new URL('/zh/tools/romance-match-quiz', SITE.url).href },
  ] },
  { '@context': 'https://schema.org', '@type': 'WebApplication', name: 'Moonlight Peaks 恋爱契合度测验',
    applicationCategory: 'GameApplication', operatingSystem: 'Web',
    url: new URL('/zh/tools/romance-match-quiz', SITE.url).href,
    description: '一个用真实、有出处的性格线索,把你与Moonlight Peaks恋爱对象配对的趣味测验——不使用编造的契合度数据。' },
];
---
<Base
  title="Moonlight Peaks 恋爱契合度测验 — 找到你的另一半"
  description="一个有趣又诚实的测验,用真实、有出处的角色线索,把你与一位Moonlight Peaks恋爱对象配对——不编造礼物或契合度数据。"
  path="/zh/tools/romance-match-quiz"
  jsonLd={jsonLd}
  crumbs={[{ label: '工具', href: '/zh/tools' }, { label: '恋爱契合度测验' }]}
  lang="zh"
>
  <article class="page">
    <div class="wrap">
      <p class="eyebrow">工具 · 恋爱</p>
      <h1>恋爱<em>契合度</em>测验</h1>
      <p class="intro">
        六个简短的问题,对照来自开发者爆料、实机试玩媒体报道和发售前泄露文件的真实性格线索。
        只是娱乐用途——下方FAQ会解释这是什么(以及不是什么)。
      </p>

      <div id="quiz-root" class="quiz-root">
        <div id="quiz-questions"></div>
        <div id="quiz-result" class="quiz-result" hidden></div>
        <div class="quiz-controls">
          <button type="button" id="quiz-restart" class="rq-btn" hidden>再试一次</button>
        </div>
      </div>

      <h2>FAQ</h2>
      <div class="faq">
        {faq.map((f) => (
          <details>
            <summary>{f.q}</summary>
            <p>{f.a}</p>
          </details>
        ))}
      </div>

      <div class="callout wip">
        相关:<a href="/tools/romance-gift-finder">恋爱礼物查询器</a>、
        <a href="/romance">恋爱与婚姻</a>、<a href="/characters">全部角色</a>。
      </div>
    </div>
  </article>

  <style>
    .quiz-root { margin: 18px 0 28px; }
    .rq-question { border: 1px solid var(--line); border-radius: 14px; background: var(--surface); padding: 16px; margin-bottom: 14px; }
    .rq-question h3 { font-family: var(--display); font-weight: 600; font-size: 1rem; margin: 0 0 12px; color: var(--mist); }
    .rq-options { display: grid; grid-template-columns: 1fr; gap: 8px; }
    @media (min-width: 640px) { .rq-options { grid-template-columns: 1fr 1fr; } }
    .rq-option {
      text-align: left; font-size: 0.86rem; color: var(--muted); border: 1px solid var(--line);
      border-radius: 10px; background: var(--ink-2); padding: 10px 12px; cursor: pointer;
      transition: border-color .15s ease, color .15s ease, background .15s ease;
    }
    .rq-option:hover { border-color: color-mix(in srgb, var(--candle) 45%, var(--line)); color: var(--mist); }
    .rq-option.is-picked { border-color: var(--candle); color: var(--candle); background: color-mix(in srgb, var(--candle) 10%, var(--ink-2)); }
    .quiz-result { border: 1px solid color-mix(in srgb, var(--candle) 40%, var(--line)); border-radius: 16px; background: var(--surface); padding: 20px; text-align: center; }
    .quiz-result h2 { font-family: var(--display); font-size: 1.3rem; margin: 4px 0 6px; }
    .quiz-result .qr-tagline { color: var(--candle); font-size: 0.94rem; margin: 0 0 12px; }
    .quiz-result .qr-blurb { color: var(--muted); font-size: 0.88rem; line-height: 1.6; max-width: 480px; margin: 0 auto 16px; }
    .quiz-result a.qr-profile { display: inline-block; font-family: var(--mono); font-size: 0.8rem; color: var(--candle); border: 1px solid color-mix(in srgb, var(--candle) 45%, var(--line)); border-radius: 999px; padding: 8px 18px; }
    .quiz-controls { text-align: center; margin-top: 14px; }
    .rq-btn { font-family: var(--mono); font-size: 0.78rem; color: var(--muted); border: 1px solid var(--line); border-radius: 999px; background: var(--surface); padding: 8px 18px; cursor: pointer; }
    .rq-btn:hover { color: var(--candle); border-color: color-mix(in srgb, var(--candle) 45%, var(--line)); }
    .rq-progress { font-family: var(--mono); font-size: 0.68rem; color: var(--faint); margin-bottom: 6px; }
  </style>

  <script define:vars={{ questions, results, cast: cast.map((c) => ({ id: c!.id, name: c!.name })) }}>
    (function () {
      var qWrap = document.getElementById('quiz-questions');
      var resultBox = document.getElementById('quiz-result');
      var restartBtn = document.getElementById('quiz-restart');
      var current = 0;
      var scores = {};

      var profileHref = {};
      cast.forEach(function (c) { profileHref[c.id] = '/characters/' + c.id; });

      function renderQuestion(i) {
        qWrap.innerHTML = '';
        var q = questions[i];
        var box = document.createElement('div');
        box.className = 'rq-question';
        var progress = document.createElement('div');
        progress.className = 'rq-progress';
        progress.textContent = '第 ' + (i + 1) + ' / ' + questions.length + ' 题';
        var h3 = document.createElement('h3');
        h3.textContent = q.q;
        var opts = document.createElement('div');
        opts.className = 'rq-options';
        q.options.forEach(function (opt) {
          var btn = document.createElement('button');
          btn.type = 'button';
          btn.className = 'rq-option';
          btn.textContent = opt.t;
          btn.addEventListener('click', function () {
            scores[opt.s] = (scores[opt.s] || 0) + 1;
            current++;
            if (current < questions.length) { renderQuestion(current); }
            else { showResult(); }
          });
          opts.appendChild(btn);
        });
        box.appendChild(progress);
        box.appendChild(h3);
        box.appendChild(opts);
        qWrap.appendChild(box);
      }

      function showResult() {
        var best = null, bestScore = -1;
        Object.keys(scores).forEach(function (k) {
          if (scores[k] > bestScore) { bestScore = scores[k]; best = k; }
        });
        if (!best) best = 'mina';
        var info = results[best];
        var name = (cast.filter(function (c) { return c.id === best; })[0] || {}).name || best;

        qWrap.style.display = 'none';
        resultBox.hidden = false;
        resultBox.innerHTML =
          '<p class="rq-progress">你的配对</p>' +
          '<h2>' + name + '</h2>' +
          '<p class="qr-tagline">' + info.tagline + '</p>' +
          '<p class="qr-blurb">' + info.blurb + '</p>' +
          '<a class="qr-profile" href="' + profileHref[best] + '">查看' + name + '的档案</a>';
        restartBtn.hidden = false;
      }

      restartBtn.addEventListener('click', function () {
        current = 0; scores = {};
        qWrap.style.display = '';
        resultBox.hidden = true;
        restartBtn.hidden = true;
        renderQuestion(0);
      });

      renderQuestion(0);
    })();
  </script>
</Base>
